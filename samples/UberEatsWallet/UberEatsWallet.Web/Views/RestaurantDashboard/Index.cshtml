@model OrdersPageViewModel
@{
    ViewData["Title"] = "Dashboard";
}

<div class="flex items-center justify-between">
    <h1 class="text-2xl font-bold">Incoming orders</h1>
    <span class="text-xs text-slate-400">Live · updates automatically</span>
</div>

<div class="mt-6" hx-get="@Url.Action("List", "RestaurantDashboard")" hx-trigger="every 3s" hx-swap="innerHTML">
    <partial name="_IncomingList" model="Model" />
</div>
