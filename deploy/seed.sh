#!/usr/bin/env bash
# Placeholder seed script. Will insert system accounts (@world, @held_pool)
# and create a demo tenant once the Ledger migrations are in place.
# Run after `docker compose up` has brought Postgres to healthy state.

set -euo pipefail

echo "seed.sh — not yet implemented (waiting on Ledger migrations)."
exit 0
